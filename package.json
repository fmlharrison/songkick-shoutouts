{
  "name": "songkick-shoutouts-express",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "cd client && yarn run dev",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\"",
    "test": "cd client && yarn test"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.2",
    "firebase-admin": "^6.0.0"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
